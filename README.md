# Squares API Implementation

## Starting Docker Container

1. Open Terminal
2. Navigate to root folder (where .sln file is located)
3. Run command `docker-compose up --build`
4. Wait until containers start up
5. Check [http://localhost:8080/swagger/index.html](http://localhost:8080/swagger/index.html), it should display the APIs documentation and endpoints that are available.
